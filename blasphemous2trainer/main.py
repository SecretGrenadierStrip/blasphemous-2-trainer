[
    {
        "path": "blasphemous-2-trainer/requirements.txt",
        "content": "keyboard==0.13.5\npsutil==5.9.8\n"
    },
    {
        "path": "blasphemous-2-trainer/src/__init__.py",
        "content": ""
    },
    {
        "path": "blasphemous-2-trainer/src/memory_reader.py",
        "content": "\"\"\"Module for reading and writing process memory for Blasphemous 2.\"\"\"\n\nimport ctypes\nimport ctypes.wintypes\nfrom typing import Optional, List, Tuple\n\n# Windows API constants\nPROCESS_VM_READ = 0x0010\nPROCESS_VM_WRITE = 0x0020\nPROCESS_VM_OPERATION = 0x0008\nPROCESS_QUERY_INFORMATION = 0x0400\n\n\nclass MemoryReader:\n    \"\"\"Handles low-level memory operations on a target process.\"\"\"\n\n    def __init__(self, process_name: str = \"Blasphemous2.exe\"):\n        self.process_name = process_name\n        self.process_handle: Optional[int] = None\n        self._kernel32 = ctypes.windll.kernel32\n\n    def open_process(self) -> bool:\n        \"\"\"Open the target process by name with VM read/write permissions.\"\"\"\n        pid = self._find_process_id()\n        if pid is None:\n            return False\n        self.process_handle = self._kernel32.OpenProcess(\n            PROCESS_VM_READ | PROCESS_VM_WRITE | PROCESS_VM_OPERATION | PROCESS_QUERY_INFORMATION,\n            False,\n            pid\n        )\n        return self.process_handle is not None and self.process_handle > 0\n\n    def _find_process_id(self) -> Optional[int]:\n        \"\"\"Enumerate processes to find Blasphemous 2's PID.\"\"\"\n        import psutil\n        for proc in psutil.process_iter(['name', 'pid']):\n            if proc.info['name'] == self.process_name:\n                return proc.info['pid']\n        return None\n\n    def read_int32(self, address: int) -> Optional[int]:\n        \"\"\"Read a 4-byte signed integer from the process memory.\"\"\"\n        if not self.process_handle:\n            return None\n        buffer = ctypes.c_int32()\n        bytes_read = ctypes.c_size_t()\n        success = self._kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            ctypes.byref(buffer),\n            ctypes.sizeof(buffer),\n            ctypes.byref(bytes_read)\n        )\n        if success and bytes_read.value == ctypes.sizeof(buffer):\n            return buffer.value\n        return None\n\n    def write_int32(self, address: int, value: int) -> bool:\n        \"\"\"Write a 4-byte signed integer to the process memory.\"\"\"\n        if not self.process_handle:\n            return False\n        buffer = ctypes.c_int32(value)\n        bytes_written = ctypes.c_size_t()\n        success = self._kernel32.WriteProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            ctypes.byref(buffer),\n            ctypes.sizeof(buffer),\n            ctypes.byref(bytes_written)\n        )\n        return success and bytes_written.value == ctypes.sizeof(buffer)\n\n    def read_float(self, address: int) -> Optional[float]:\n        \"\"\"Read a 4-byte float from the process memory.\"\"\"\n        if not self.process_handle:\n            return None\n        buffer = ctypes.c_float()\n        bytes_read = ctypes.c_size_t()\n        success = self._kernel32.ReadProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            ctypes.byref(buffer),\n            ctypes.sizeof(buffer),\n            ctypes.byref(bytes_read)\n        )\n        if success and bytes_read.value == ctypes.sizeof(buffer):\n            return buffer.value\n        return None\n\n    def write_float(self, address: int, value: float) -> bool:\n        \"\"\"Write a 4-byte float to the process memory.\"\"\"\n        if not self.process_handle:\n            return False\n        buffer = ctypes.c_float(value)\n        bytes_written = ctypes.c_size_t()\n        success = self._kernel32.WriteProcessMemory(\n            self.process_handle,\n            ctypes.c_void_p(address),\n            ctypes.byref(buffer),\n            ctypes.sizeof(buffer),\n            ctypes.byref(bytes_written)\n        )\n        return success and bytes_written.value == ctypes.sizeof(buffer)\n\n    def close(self):\n        \"\"\"Close the handle to the target process.\"\"\"\n        if self.process_handle:\n            self._kernel32.CloseHandle(self.process_handle)\n            self.process_handle = None\n"
    },
    {
        "path": "blasphemous-2-trainer/src/address_map.py",
        "content": "\"\"\"Static memory addresses for Blasphemous 2 (version 1.0.5).\n\nThese offsets are relative to the base address of the game module.\nThey are discovered via pattern scanning and may need updating after patches.\n\"\"\"\n\nfrom dataclasses import dataclass\nfrom typing import Dict, Any\n\n\n@dataclass\nclass AddressEntry:\n    \"\"\"Represents a known memory address offset and its description.\"\"\"\n    offset: int\n    size: int  # in bytes\n    description: str\n    data_type: str  # 'int32' or 'float'\n\n\nclass AddressMap:\n    \"\"\"Central registry of all cheat-related memory addresses.\"\"\"\n\n    def __init__(self):\n        self._addresses: Dict[str, AddressEntry] = {\n            # Player health (float, current health)\n            \"health\": AddressEntry(\n                offset=0x00A3B7C0,\n                size=4,\n                description=\"Current player health\",\n                data_type=\"float\"\n            ),\n            # Max health (float)\n            \"max_health\": AddressEntry(\n                offset=0x00A3B7C4,\n                size=4,\n                description=\"Maximum player health\",\n                data_type=\"float\"\n            ),\n            # Fervour (float, mana/energy)\n            \"fervour\": AddressEntry(\n                offset=0x00A3B7D0,\n                size=4,\n                description=\"Current fervour (mana)\",\n                data_type=\"float\"\n            ),\n            # Tears of Atonement (int32, currency)\n            \"tears\": AddressEntry(\n                offset=0x00A3B7E0,\n                size=4,\n                description=\"Tears of Atonement currency\",\n                data_type=\"int32\"\n            ),\n            # Attack damage multiplier (float)\n            \"damage_mult\": AddressEntry(\n                offset=0x00A3B7F0,\n                size=4,\n                description=\"Attack damage multiplier\",\n                data_type=\"float\"\n            ),\n        }\n\n    def get(self, name: str) -> AddressEntry:\n        \"\"\"Get an address entry by its symbolic name.\"\"\"\n        if name not in self._addresses:\n            raise KeyError(f\"Unknown address name: {name}\")\n        return self._addresses[name]\n\n    def list_all(self) -> Dict[str, AddressEntry]:\n        \"\"\"Return all address entries.\"\"\"\n        return dict(self._addresses)\n\n    def update_offset(self, name: str, new_offset: int):\n        \"\"\"Update an offset (e.g., after a game patch).\"\"\"\n        if name in self._addresses:\n            self._addresses[name].offset = new_offset\n"
    },
    {
        "path": "blasphemous-2-trainer/src/trainer.py",
        "content": "\"\"\"Main trainer logic: hotkeys and cheat toggles.\"\"\"\n\nimport time\nimport threading\nimport keyboard\nfrom src.memory_reader import MemoryReader\nfrom src.address_map import AddressMap, AddressEntry\n\n\nclass Blasphemous2Trainer:\n    \"\"\"Trainer class providing cheats for Blasphemous 2.\"\"\"\n\n    def __init__(self):\n        self.memory = MemoryReader()\n        self.addr_map = AddressMap()\n        self._running = False\n        self._thread: threading.Thread = None\n        self._cheats_active = {\n            \"god_mode\": False,\n            \"infinite_fervour\": False,\n            \"max_tears\": False,\n        }\n\n    def start(self) -> bool:\n        \"\"\"Attempt to attach to the game process and start the trainer loop.\"\"\"\n        if not self.memory.open_process():\n            print(\"Failed to attach to Blasphemous 2 process. Is the game running?\")\n            return False\n        print(\"Attached to Blasphemous 2.\")\n        self._running = True\n        self._thread = threading.Thread(target=self._trainer_loop, daemon=True)\n        self._thread.start()\n        self._register_hotkeys()\n        return True\n\n    def stop(self):\n        \"\"\"Stop the trainer and clean up.\"\"\"\n        self._running = False\n        if self._thread and self._thread.is_alive():\n            self._thread.join(timeout=1.0)\n        self.memory.close()\n        print(\"Trainer stopped.\")\n\n    def _register_hotkeys(self):\n        \"\"\"Register keyboard hotkeys for toggling cheats.\"\"\"\n        keyboard.add_hotkey('F1', self._toggle_god_mode)\n        keyboard.add_hotkey('F2', self._toggle_infinite_fervour)\n        keyboard.add_hotkey('F3', self._toggle_max_tears)\n        keyboard.add_hotkey('F10', self.stop)\n        print(\"Hotkeys: F1=God Mode, F2=Infinite Fervour, F3=Max Tears, F10=Exit\")\n\n    def _toggle_god_mode(self):\n        \"\"\"Toggle god mode (freeze health at max).\"\"\"\n        self._cheats_active[\"god_mode\"] = not self._cheats_active[\"god_mode\"]\n        status = \"ON\" if self._cheats_active[\"god_mode\"] else \"OFF\"\n        print(f\"God Mode: {status}\")\n\n    def _toggle_infinite_fervour(self):\n        \"\"\"Toggle infinite fervour (freeze at max).\"\"\"\n        self._cheats_active[\"infinite_fervour\"] = not self._cheats_active[\"infinite_fervour\"]\n        status = \"ON\" if self._cheats_active[\"infinite_fervour\"] else \"OFF\"\n        print(f\"Infinite Fervour: {status}\")\n\n    def _toggle_max_tears(self):\n        \"\"\"Toggle max tears (set to 99999 each tick).\"\"\"\n        self._cheats_active[\"max_tears\"] = not self._cheats_active[\"max_tears\"]\n        status = \"ON\" if self._cheats_active[\"max_tears\"] else \"OFF\"\n        print(f\"Max Tears: {status}\")\n\n    def _trainer_loop(self):\n        \"\"\"Main loop that applies cheats each tick.\"\"\"\n        while self._running:\n            try:\n                if self._cheats_active[\"god_mode\"]:\n                    self._apply_god_mode()\n                if self._cheats_active[\"infinite_fervour\"]:\n                    self._apply_infinite_fervour()\n                if self._cheats_active[\"max_tears\"]:\n                    self._apply_max_tears()\n            except Exception as e:\n                print(f\"Error in trainer loop: {e}\")\n            time.sleep(0.05)  # 20 Hz update rate\n\n    def _apply_god_mode(self):\n        \"\"\"Set health to max health.\"\"\"\n        max_hp_entry = self.addr_map.get(\"max_health\")\n        health_entry = self.addr_map.get(\"health\")\n        max_hp = self.memory.read_float(max_hp_entry.offset)\n        if max_hp is not None:\n            self.memory.write_float(health_entry.offset, max_hp)\n\n    def _apply_infinite_fervour(self):\n        \"\"\"Set fervour to a large value.\"\"\"\n        fervour_entry = self.addr_map.get(\"fervour\")\n        # Max fervour is typically 100.0, but we write 100.0 to be safe\n        self.memory.write_float(fervour_entry.offset, 100.0)\n\n    def _apply_max_tears(self):\n        \"\"\"Set tears to 99999.\"\"\"\n        tears_entry = self.addr_map.get(\"tears\")\n        self.memory.write_int32(tears_entry.offset, 99999)\n"
    },
    {
        "path": "blasphemous-2-trainer/src/config.py",
        "content": "\"\"\"Configuration management for the trainer.\"\"\"\n\nimport json\nimport os\nfrom typing import Dict, Any\n\nDEFAULT_CONFIG_PATH = \"trainer_config.json\"\n\n\nclass TrainerConfig:\n    \"\"\"Handles saving/loading trainer settings to a JSON file.\"\"\"\n\n    def __init__(self, config_path: str = DEFAULT_CONFIG_PATH):\n        self.config_path = config_path\n        self.data: Dict[str, Any] = {\n            \"hotkeys\": {\n                \"god_mode\": \"F1\",\n                \"infinite_fervour\": \"F2\",\n                \"max_tears\": \"F3\",\n                \"exit\": \"F10\"\n            },\n            \"update_rate_hz\": 20,\n            \"auto_attach\": True\n        }\n\n    def load(self):\n        \"\"\"Load config from file if it exists.\"\"\"\n        if os.path.exists(self.config_path):\n            try:\n                with open(self.config_path, 'r') as f:\n                    loaded = json.load(f)\n                    self.data.update(loaded)\n            except (json.JSONDecodeError, IOError):\n                pass\n\n    def save(self):\n        \"\"\"Save current config to file.\"\"\"\n        with open(self.config_path, 'w') as f:\n            json.dump(self.data, f, indent=4)\n\n    def get_hotkey(self, cheat_name: str) -> str:\n        \"\"\"Get hotkey for a given cheat.\"\"\"\n        return self.data.get(\"hotkeys\", {}).get(cheat_name, \"unknown\")\n\n    def set_hotkey(self, cheat_name: str, key: str):\n        \"\"\"Set a hotkey for a cheat.\"\"\"\n        if \"hotkeys\" not in self.data:\n            self.data[\"hotkeys\"] = {}\n        self.data[\"hotkeys\"][cheat_name] = key\n"
    },
    {
        "path": "blasphemous-2-trainer/main.py",
        "content": "\"\"\"Entry point for the Blasphemous 2 Trainer.\"\"\"\n\nimport sys\nimport time\nfrom src.trainer import Blasphemous2Trainer\nfrom src.config import TrainerConfig\n\n\ndef main():\n    \"\"\"Launch the trainer.\"\"\"\n    print(\"Blasphemous 2 Trainer v1.0\")\n    print(\"=\" * 30)\n\n    config = TrainerConfig()\n    config.load()\n\n    trainer = Blasphemous2Trainer()\n    if not trainer.start():\n        print(\"Could not start trainer. Exiting.\")\n        sys.exit(1)\n\n    try:\n        while True:\n            time.sleep(1)\n    except KeyboardInterrupt:\n        print(\"\\nShutting down...\")\n    finally:\n        trainer.stop()\n        config.save()\n\n\nif __name__ == \"__main__\":\n    main()\n"
    },
    {
        "path": "blasphemous-2-trainer/tests/__init__.py",
        "content": ""
    },
    {
        "path": "blasphemous-2-trainer/tests/test_address_map.py",
        "content": "\"\"\"Unit tests for the AddressMap class.\"\"\"\n\nimport unittest\nfrom src.address_map import AddressMap, AddressEntry\n\n\nclass TestAddressMap(unittest.TestCase):\n    \"\"\"Test cases for AddressMap.\"\"\"\n\n    def setUp(self):\n        self.addr_map = AddressMap()\n
